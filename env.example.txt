# =============================================================================
# PPC Flight Recorder – Environment & Credentials
# =============================================================================
# Copy this file to .env in this folder and set your values. Do not commit .env.

# ----- Snowflake (key-pair recommended to avoid MFA/TOTP) -----
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_AUTH_METHOD=KEYPAIR
# For KEYPAIR: set either SNOWFLAKE_PRIVATE_KEY (inline PEM) or SNOWFLAKE_PRIVATE_KEY_PATH (file)
# In .env, for inline key use \n for newlines: -----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----
SNOWFLAKE_PRIVATE_KEY=
# SNOWFLAKE_PRIVATE_KEY_PATH=path/to/private_key.p8
# SNOWFLAKE_PRIVATE_KEY_PASSPHRASE=
# For PASSWORD auth instead: SNOWFLAKE_AUTH_METHOD=PASSWORD and set SNOWFLAKE_PASSWORD=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_WAREHOUSE=
SNOWFLAKE_DATABASE=
SNOWFLAKE_SCHEMA=
# SNOWFLAKE_ROLE=

# ----- Google Ads API -----
# If using a proxy, set http_proxy and https_proxy with scheme, e.g. http://127.0.0.1:3213 (not 127.0.0.1:3213)
GOOGLE_ADS_DEVELOPER_TOKEN=
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=
GOOGLE_ADS_REFRESH_TOKEN=
GOOGLE_ADS_LOGIN_CUSTOMER_ID=

# Customer ID per project (use fallback GOOGLE_ADS_CUSTOMER_ID if only one account)
GOOGLE_ADS_CUSTOMER_ID=
GOOGLE_ADS_CUSTOMER_ID_THEPINCH=
GOOGLE_ADS_CUSTOMER_ID_THENICKEL=
GOOGLE_ADS_CUSTOMER_ID_THEQUOIN=
GOOGLE_ADS_CUSTOMER_ID_ANTHOLOGY=
GOOGLE_ADS_CUSTOMER_ID_MYROOST=
GOOGLE_ADS_CUSTOMER_ID_WM_MULHERINS_SONS=2507533927

# ----- GA4 (Apps Script URL for traffic acquisition) -----
# Deploy your Apps Script as web app; put the "exec" URL here.
GA4_MARKETING_API_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
# Set to 1/true/yes to fetch and save GA4 data on daily (scheduled and manual) sync. Default: off.
# SAVE_GA4_ON_DAILY_SYNC=1

# ----- Mews PMS Flight Recorder (Connector API; self-contained) -----
# MEWS_BASE_URL=https://api.mews.com/api/connector/v1
MEWS_CLIENT_TOKEN=
MEWS_ACCESS_TOKEN=
# MEWS_CLIENT_NAME=PPC Flight Recorder
# Optional when using portfolio / multi-enterprise tokens:
# MEWS_ENTERPRISE_ID=
# Chain ID is read from configuration/get (Enterprise.ChainId), not from env.
# Connect vs read timeouts (seconds): read raised for slow orderitems/bills responses.
# MEWS_REQUEST_CONNECT_TIMEOUT_SEC=30
# MEWS_REQUEST_TIMEOUT_SEC=600
# MEWS_REQUEST_RETRIES=5
# MEWS_RETRY_BACKOFF_SEC=2.0
# MEWS_RETRY_BACKOFF_MAX_SEC=60.0
# MEWS_PAGE_SIZE=1000
# Max ServiceIds / CustomerIds / etc. per Connector request (Mews often caps at 1000); we chunk and merge.
# MEWS_MAX_BATCH_IDS=1000
# Days before snapshot date (UTC) for UpdatedUtc lower bound; upper bound is end of snapshot day.
# MEWS_SNAPSHOT_DAYS_BACK=365
# Legacy name still read if MEWS_SNAPSHOT_DAYS_BACK unset: MEWS_RESERVATION_DAYS_BACK
# UpdatedUtc slice length in days (capped at Mews 3M1D per request). 0 = 90-day chunks.
# MEWS_TIME_SLICE_DAYS=7
# MEWS_AVAILABILITY_BLOCK_HOURS=99
# Non-core APIs: UpdatedUtc from this date (UTC midnight) through end of snapshot_date (--date),
# not “today” when backfilling. Core transactional APIs (reservations, productServiceOrders,
# orderItems, payments, bills, availabilityBlocks) use MEWS_SNAPSHOT_DAYS_BACK. companionships
# and sourceassignments use MEWS_SNAPSHOT_DAYS_BACK only; they do not write *_diff_daily.
# MEWS_ALL_FETCH_DATA_START_DATE=2021-01-01
# Rows per Snowflake write_pandas batch for pms_mews_* loads (default 5000)
# Rows per write_pandas batch (default 1000). Lower if you see OSError 28 / no space on device during Snowflake upload
# (connector writes encrypted Parquet under %TEMP%). Free disk or set TMPDIR to a larger drive; raise batch if plenty of space.
# MEWS_SNOWFLAKE_WRITE_BATCH_ROWS=1000

# ----- PPC Flight Recorder -----
# Comma-separated projects to sync when --project is not passed (default: the-pinch)
# Example full list: the-pinch,the-nickel,the-quoin,anthology,myroost-com,wm-mulherins-sons
PPC_PROJECTS=the-pinch

# Daily sync scheduler (server only): timezone and local time (24h). Default 9:30 PM EST.
SYNC_SCHEDULE_TIMEZONE=America/New_York
SYNC_SCHEDULE_HOUR=21
SYNC_SCHEDULE_MINUTE=30
# After PPC daily sync (scheduler + POST /sync), run Mews PMS flight recorder for the same snapshot date. Default on; set 0/false/no to skip.
# RUN_MEWS_SYNC_AFTER_DAILY_SYNC=1
# When 1/true/yes, Mews post-sync also writes pms_mews_*_diff_daily (same as python sync_mews.py --diff).
# MEWS_SYNC_DO_DIFF_ON_SCHEDULE=0

# ----- IDeaS G3 Flight Recorder (SFTPCloud) -----
IDEAS_SFTP_HOST=us-east-1.sftpcloud.io
IDEAS_SFTP_PORT=22
# IDEAS_FTPS_PORT=21
IDEAS_SFTP_USERNAME=
IDEAS_SFTP_PASSWORD=
IDEAS_SFTP_REMOTE_DIR=/
# IDEAS_SFTP_PROTOCOL=auto
# IDEAS_SFTP_MAX_RETRIES=3
# IDEAS_SFTP_RETRY_BACKOFF_SEC=2.0
# IDeaS scheduler (server only): default 9:00 AM — syncs yesterday + today delivery dates (unlike PPC/Mews which use yesterday only).
# IDEAS_SYNC_SCHEDULE_TIMEZONE=America/New_York
# IDEAS_SYNC_SCHEDULE_HOUR=9
# IDEAS_SYNC_SCHEDULE_MINUTE=0
# RUN_IDEAS_SYNC_ON_SCHEDULE=1